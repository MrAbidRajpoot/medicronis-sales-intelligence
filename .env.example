# Database (Neon / Supabase / local Docker)
DATABASE_URL="postgresql://medicronis:medicronis@localhost:5432/medicronis?schema=public"

# Auth — shared demo password (login page)
DEMO_PASSWORD="demo"

# PDF worker (Railway / Render URL in production)
PDF_WORKER_URL="http://localhost:8000"

# File storage — local path for dev; /tmp on Vercel (ephemeral)
UPLOAD_DIR="./uploads"
